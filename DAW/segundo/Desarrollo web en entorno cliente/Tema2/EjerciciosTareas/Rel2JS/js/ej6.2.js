function ej62() {
    var a = prompt("Introduzca su contraseña:");
    var b = prompt("Vuelva a introducir su contraseña:");
    if (a == b) {
        alert("Las contraseñas coinciden");
    } else {
        alert("Las contraseñas no coinciden");
    }
}